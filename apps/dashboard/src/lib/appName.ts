/**
 * The product name every surface spells the same way.
 *
 * It lives here rather than in `providers.tsx` because the auth frame renders
 * the wordmark beside the branding logo, and two literals in two files is how
 * a rename ends up half-applied.
 */
export const SCHOOL_APP_NAME = 'MyScolAI';
